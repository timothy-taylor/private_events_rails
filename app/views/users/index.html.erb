<h1>Users</h1>
<ul>
<% @users.each do |user| %>
  <li><%= link_to user.email, user %>; since:
  <%= user.created_at %></li>
<% end %>
</ul>

<div class="spacer"></div>
<%= link_to 'Back', events_path %>
